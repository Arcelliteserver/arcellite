# ─────────────────────────────────────────────────────────────
# Arcellite — Environment Configuration (Example)
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# Or run ./install.sh to generate it automatically.
# ─────────────────────────────────────────────────────────────

# PostgreSQL Database
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=arcellite
DB_USER=arcellite_user
DB_PASSWORD=your_database_password_here

# MySQL/MariaDB (optional — only needed if using MySQL databases in the Database Manager)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=arcellite_user
MYSQL_PASSWORD=your_mysql_password_here
MYSQL_ROOT_PASSWORD=your_mysql_root_password_here

# Application
SESSION_SECRET=generate_a_random_64_char_hex_string_here
NODE_ENV=development
# CORS (optional — only set if you need cross-origin API access from a specific domain)
# ALLOWED_ORIGIN=http://your-domain.com

# Data directory (where files, photos, videos, music are stored)
ARCELLITE_DATA=~/arcellite-data

# Email (optional — for account verification codes and notifications)
# Configure with your SMTP provider. Port 465 = SSL, Port 587 = STARTTLS.
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your-email-password
SMTP_FROM=Arcellite <noreply@example.com>

# AI Email (optional — for AI assistant email features)
AI_SMTP_HOST=smtp.example.com
AI_SMTP_PORT=465
AI_SMTP_SECURE=true
AI_SMTP_USER=your-email@example.com
AI_SMTP_PASSWORD=your-email-password
AI_SMTP_FROM=Arcellite AI <assistant@example.com>
AI_NOREPLY_FROM=Arcellite <noreply@example.com>
